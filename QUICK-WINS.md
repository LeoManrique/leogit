# leogit — Quick Wins (small effort, high payoff)

Small day-to-day polish toward lms-github-desktop parity.

Big features (PR/CI, clone, stash, native menus, rebase, auto-update) are intentionally OUT of scope and tracked in ROADMAP.md.

## Already built — just switch it on

These are the cheapest wins: the backend/handlers/UI already exist and only need wiring. Do these first.

- [ ] **Open the already-wired Merge overlay from the branch picker** `[already built]` — pick a branch and merge it (regular or squash) into the current branch; the entire merge feature is dead code today solely because nothing ever sets `mergeTarget`/`showMerge=true`.
  - _Change:_ In BranchDropdown.svelte add an `onMerge(name)` prop and a per-branch "Merge into current" affordance (hover button or context item) for non-current local branches; in MainLayout add `handleMergeRequest(name){ mergeTarget=name; showMerge=true; showBranches=false }` and pass `onMerge`. Handlers `handleMerge`/`handleSquashMerge` and `MergeOverlay` already exist. MergeOverlay labels it "Merge {source} into {target}", matching source=mergeTarget, target=current.  ·  _Files:_ `tauri-app/src/lib/views/MainLayout.svelte`, `tauri-app/src/lib/views/BranchDropdown.svelte`  ·  _Effort:_ small
- [ ] **Live merge-commit preview count in MergeOverlay** `[already built]` — show "This will bring in N commits from {source}" so the user sees the merge size before confirming; `countCommitsToMerge` is wrapped and implemented but never called.
  - _Change:_ In MainLayout `handleMergeRequest`, after setting mergeTarget, `mergeCount = await gitApi.countCommitsToMerge(repoPath, mergeTarget)` into `let mergeCount = $state<number|null>(null)` and pass `commitCount={mergeCount}` to MergeOverlay. In MergeOverlay add `commitCount?: number|null` prop and render "This will bring in {n} commit(s)" / "Already up to date — nothing to merge" when 0 (optionally disable Merge). Reset on close. NOTE: `count_commits_to_merge`'s arg is measured against HEAD, so pass the source/mergeTarget branch. Pairs with the merge-trigger item — do them together.  ·  _Files:_ `tauri-app/src/lib/views/MainLayout.svelte`, `tauri-app/src/lib/views/MergeOverlay.svelte`  ·  _Effort:_ small
- [ ] **Persistent Undo-last-commit banner in the Changes view** `[already built]` — "Committed <relative time> — <summary> [Undo]" banner above the commit composer for the most recent local (unpushed) commit; today undo is only reachable via right-click in History.
  - _Change:_ Render a small UndoCommitBanner (or inline markup) at the top of the Changes tab-pane, above file-list-container. Derive `undoableCommit = log.commits[0]`, shown only when not amending/committing and `(!hasResolvedUpstream || unpushedShas.has(commits[0].sha))` — reuse CommitList's exact gating. Its Undo button calls the existing `handleUndoCommit(commits[0])`; reuse `CommitList.formatDate` (factor into a tiny shared helper). Keep v1 to the unpushed gating (skip the push/pull-in-flight guard for now).  ·  _Files:_ `tauri-app/src/lib/views/MainLayout.svelte`, `tauri-app/src/lib/components/CommitList.svelte`  ·  _Effort:_ small-medium
- [ ] **"Create branch from this commit" in the commit context menu** `[already built]` — branch off any past commit's exact SHA without checking it out first; `create_branch` already accepts `start_point` but `handleCreateBranch` always passes `''`.
  - _Change:_ CommitList.svelte: add `onCreateBranchFrom?(commit)` prop + a "Create branch from commit…" ContextMenuItem after the separator. MainLayout: add `handleCreateBranchFromCommit` that prompts for a name (reuse BranchDropdown's inline input), then `gitApi.createBranch(repoPath, name, commit.sha)` + switchBranch + refreshStatus/Branches/Log. Backend needs zero changes.  ·  _Files:_ `tauri-app/src/lib/components/CommitList.svelte`, `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium
- [ ] **Refresh History/branches immediately after merge** `[already built]` — surface the merge commit and updated graph instantly instead of waiting for the next 2s poll; `handleMerge`/`handleSquashMerge` refresh status but not branches.
  - _Change:_ Add `await refreshBranches()` alongside the existing `refreshStatus()`/`handleCommitted()` calls in both `handleMerge` and `handleSquashMerge`. One line each, reusing the existing function. (Other handlers already do this — it's just inconsistent here.)  ·  _Files:_ `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small
- [ ] **Refresh history + branches inline after push/pull (kill the 2s lag)** `[already built]` — History and the unpushed-commit dots update the instant a push/pull finishes instead of lagging up to 2s; push/pull only re-run `get_status` today.
  - _Change:_ Add `onSynced?()` prop to Header (wired in MainLayout to `Promise.all([refreshLog(), refreshBranches()])` + update lastHeadSha) and call it at the end of `handlePull`/`handlePush`/`handleForcePush` after `handleRefresh`. Keep the refresh logic in MainLayout — don't re-implement log paging in Header (DRY).  ·  _Files:_ `tauri-app/src/lib/components/Header.svelte`, `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small
- [ ] **Wire ErrorModal's Retry button to re-run the failed sync op** `[already built]` — ErrorModal already renders a Retry button when given `onRetry`, but it's never passed, so every push/pull/fetch failure is dismiss-only.
  - _Change:_ Track the last failed network action (e.g. `lastFailedAction: (()=>void)|null` on RepoState, or a `$state` closure in MainLayout). In Header's `handlePull`/`handlePush`/`handleForcePush` catch blocks (or MainLayout's merge catch blocks) set it to a closure re-invoking the op. Pass `onRetry={lastFailedAction ? () => { const a=lastFailedAction; lastFailedAction=null; dismissError(); a?.() } : undefined}` to ErrorModal; clear in `dismissError`. Scope to network ops only.  ·  _Files:_ `tauri-app/src/lib/views/MainLayout.svelte`, `tauri-app/src/lib/components/Header.svelte`, `tauri-app/src/lib/stores/repo.ts`  ·  _Effort:_ small
- [ ] **Re-arm auto-fetch interval on Settings Save (no restart needed)** `[already built]` — changing Auto fetch / Fetch interval takes effect instantly; today `startAutoFetch` only runs on initialize/repo-switch.
  - _Change:_ Add an `onSaved?()` prop to SettingsOverlay, call it in `handleSave` after `refreshConfig`. In MainLayout pass `onSaved={() => { const cfg=$config; startAutoFetch(cfg?.auto_fetch ? cfg.fetch_interval_ms||30000 : 0) }}`. Reuses the existing interval-arming expression. `startAutoFetch` already clears+rebuilds the interval.  ·  _Files:_ `tauri-app/src/lib/views/SettingsOverlay.svelte`, `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small

> Note: the merge-trigger / merge-count / push-pull-refresh / onRetry items appear once each here; their duplicate phrasings under other areas are folded in.

## Changes & commit composer

- [ ] **Commit button label "Commit to \<branch\>" + disabled-reason tooltip + in-flight spinner** — user always knows which branch they're committing to and why the button is greyed out.
  - _Change:_ In CommitMessage.svelte read `$repoState.status.branch`. Replace the static label with `${isCommitting ? (isAmending?'Amending…':'Committing…') : (isAmending?'Amend last commit':'Commit to ')}` + `<strong>{branch}</strong>` for the non-amend case. Derive `disabledReason` ("A commit summary is required" / "Select one or more files to commit" / "Committing…") and bind to `title`/`aria-label`, falling back to the shortcut hint when enabled. Add a CSS border-spin spinner span before the label gated on `isCommitting` (no new dep).  ·  _Files:_ `tauri-app/src/lib/components/CommitMessage.svelte`  ·  _Effort:_ small
- [ ] **Single-file "Create/Delete/Update \<file\>" summary placeholder + commit-when-blank** — one-file commits become zero-typing; matches GitHub Desktop muscle memory.
  - _Change:_ Derive `placeholder` from selectedFiles + status.files — when size===1, map the one file's status to Create/Delete/Update + `display_name`, else "Summary"; bind to the input placeholder. Add `effectiveSummary = summary.trim() || (selectedFiles.size===1 ? placeholder : '')`. Loosen `canSubmit` to `effectiveSummary.length>0 && (isAmending || $canCommit)`, drop the blank-check early return in `handleCommit`, and pass `effectiveSummary` into `formatCommitMessage`.  ·  _Files:_ `tauri-app/src/lib/components/CommitMessage.svelte`  ·  _Effort:_ small
- [ ] **Summary length-hint lightbulb past 50 chars** — nudges concise subject lines without nagging on every keystroke.
  - _Change:_ Add derived `showLengthHint = charCount > 50`. Render a small lightbulb glyph absolutely positioned in .summary-section (reuse the .char-count positioning pattern), shown when `showLengthHint`, with `title="Great commit summaries contain fewer than 50 characters. Place extra information in the description field."` Keep the existing /72 counter. Pure frontend.  ·  _Files:_ `tauri-app/src/lib/components/CommitMessage.svelte`  ·  _Effort:_ small
- [ ] **AI-generate overwrite confirm when a message already exists** — stops the AI button from silently wiping a half-typed commit message.
  - _Change:_ In `handleGenerate`, before the network call, if `summary.trim() || description.trim()` gate on a confirm. Prefer an in-app inline prompt: set a `pendingGenerate` flag that swaps the in-component notice region into "Replace message? [Replace] [Cancel]" to stay theme-consistent. (See the pref-gated variant under Settings & prompts — same mechanism.)  ·  _Files:_ `tauri-app/src/lib/components/CommitMessage.svelte`  ·  _Effort:_ small
- [ ] **Aggregate "N changed files" badge cap at 300+** — header stays compact on huge changesets; matches GHD's `MaximumChangesCount=300`.
  - _Change:_ Add `countLabel = files.length > 300 ? '300+' : String(files.length)` and substitute into the select-all header span: `{countLabel} changed file{files.length === 1 ? '' : 's'}`. One-line, frontend only.  ·  _Files:_ `tauri-app/src/lib/components/FileList.svelte`  ·  _Effort:_ small
- [ ] **Per-file status tooltip + full status word** — the single-letter A/M/D/R/U badge becomes self-explanatory on hover.
  - _Change:_ Add `getStatusTitle(status)` returning the full word ('New (untracked or staged add)', 'Modified', 'Deleted', 'Renamed', 'Conflicted') and set `title={getStatusTitle(file.status)}` on the .status-badge div. Optionally distinguish untracked from staged-add via `file.xy.startsWith('?')`. Frontend only; do NOT add a Copied status (not in leogit's model).  ·  _Files:_ `tauri-app/src/lib/components/FileList.svelte`  ·  _Effort:_ small
- [ ] **Warn before undo when the working tree is dirty** — prevents a confusing pile-up of staged + undone changes the user didn't expect.
  - _Change:_ In `handleUndoCommit`, before `await gitApi.undoLastCommit`, if `$repoState.status.files.length > 0` require confirmation (inline confirm state or the ErrorModal-style overlay) explaining the undone commit's files will be added to current uncommitted changes. Proceed only on confirm. Single guard, no backend. (Composes with the pref-gated undo confirm under Settings & prompts.)  ·  _Files:_ `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small

## Diff viewer

- [ ] **Brighten the dim .selection-dot border** — the per-line include affordance is legible the moment per-line include is enabled.
  - _Change:_ In .selection-dot, swap `border-color` from `var(--border-strong)` to `var(--border-active)` (or `--text-muted`) and bump to a 1.5px ring; keep the .selected fill. Pure CSS, one rule.  ·  _Files:_ `src/lib/components/DiffViewer.svelte`  ·  _Effort:_ small
- [ ] **Large-diff guard with "Show diff anyway" opt-in** — opening a giant generated/lockfile diff no longer janks the UI; user opts in.
  - _Change:_ Compute `totalLines` (sum of hunk line counts) as `$derived`. Add `let forceShowLarge = $state(false)`, reset in the per-file reset effect. When `!forceShowLarge && totalLines > 20000 && !is_binary`, render a blankslate ("The diff is too large to display by default…") in place of .diff-body with a Show-diff button. Keep .file-header (and the +/- counters) visible. Reuses .diff-empty styling.  ·  _Files:_ `src/lib/components/DiffViewer.svelte`  ·  _Effort:_ small
- [ ] **Distinct empty/special diff states** — stops the "a file is clearly selected but it says select a file" dead-end.
  - _Change:_ In MainLayout's changes branch, when `activeFile` is non-null but `activeFileDiff` is null (and not loading), render a dedicated message from `activeFile.status`: 'Renamed' → "Renamed, no content change"; else if `$config.hide_whitespace` → "Only whitespace changed (hidden)"; else "This file is empty or has no displayable changes." Mirror in the commit-diff-pane branch for `activeCommitFile`. Reuse .diff-empty styling.  ·  _Files:_ `src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium
- [ ] **Per-file diff-options gear (Split/Unified + hide-whitespace)** — flip a file to split view or hide whitespace right where you're reading, no trip to Settings.
  - _Change:_ Add a gear button to .file-header opening a popover with a Unified/Split radio + a hide-whitespace checkbox. Wire through `onToggleSideBySide`/`onToggleHideWhitespace` callbacks so MainLayout updates `$config` and calls `configApi.saveConfig` + `config.set` — identical to SettingsOverlay's writes. side_by_side flips instantly; hide_whitespace triggers the existing reload effect. Reuse ContextMenu/popover styling.  ·  _Files:_ `src/lib/components/DiffViewer.svelte`, `src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium
- [ ] **Split-view copy strips the opposite column and the +/- marker** — copying code from split diffs yields clean, paste-ready text instead of both columns interleaved with markers.
  - _Change:_ (1) Add `user-select:none` to .line-prefix (alongside .line-number) so the +/- never enters the selection — fixes unified view too. (2) Add a `copy` listener on .diff-body (only when sideBySide) that, when the selection is inside the diff, rebuilds clipboard text from the side the selection anchored in (track via pointerdown/selectionchange whether anchorNode is under .sbs-left/.sbs-right), joins that side's row.content with `\n`, and `setData('text/plain', text)` + `preventDefault()`. Mirrors GHD's `onCutOrCopy`.  ·  _Files:_ `src/lib/components/DiffViewer.svelte`  ·  _Effort:_ small-medium

## History & commit menu

- [ ] **"Copy full message" in the commit context menu** — grab a whole commit message for a PR description or changelog in one click.
  - _Change:_ Add a `copyMessage(commit)` helper writing `${commit.summary}${commit.body ? '\n\n'+commit.body : ''}` via `navigator.clipboard`, and append a ContextMenuItem next to Copy SHA. Pure frontend, no backend.  ·  _Files:_ `tauri-app/src/lib/components/CommitList.svelte`  ·  _Effort:_ small
- [ ] **Render tag chips in the commit detail pane** — see at a glance which release/tag a selected commit belongs to.
  - _Change:_ Add a `tagsFor(commit)` helper (copy the 6-line one from CommitList) and render a row of pills from `commit.refs` near the meta row, reusing the .tag-name CSS. No backend; refs already on the commit.  ·  _Files:_ `tauri-app/src/lib/views/CommitDetail.svelte`  ·  _Effort:_ small
- [ ] **Expand/collapse long commit body in commit detail** — read long messages in full without fighting a tiny inner scrollbar.
  - _Change:_ Add `let expanded = $state(false)`; apply `max-height` only when `!expanded`; render a "Show more / Show less" text button after the `<pre>`, shown only when the body overflows (measure scrollHeight>clientHeight via bind:this + $effect, or a line-count threshold). Pure CSS/state.  ·  _Files:_ `tauri-app/src/lib/views/CommitDetail.svelte`  ·  _Effort:_ small
- [ ] **Empty / no-commit-selected state in the History detail pane** — removes a confusing blank area when switching to History before clicking a commit.
  - _Change:_ Add an `{:else}` branch after the `activeCommit` block rendering a .diff-empty-style div with "Select a commit to view its details" (mirrors the Changes pane). No backend.  ·  _Files:_ `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small
- [ ] **"Revert this commit" in the commit context menu** — one-click safe undo of any historical commit without manual git.
  - _Change:_ git.rs: add `revert_commit(repo_path, sha, is_merge)` running `git revert --no-edit [-m 1 if merge] <sha>`; register in main.rs. commands.ts: add `gitApi.revertCommit`. CommitList: add `onRevertCommit` prop + "Revert changes in commit" item. MainLayout: handler computes `isMerge = commit.parents.length>1`, calls revert, then refreshStatus+refreshLog; surface errors via `repoState.error`.  ·  _Files:_ `tauri-app/src-tauri/src/commands/git.rs`, `tauri-app/src-tauri/src/main.rs`, `tauri-app/src/lib/api/commands.ts`, `tauri-app/src/lib/components/CommitList.svelte`, `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium
- [ ] **"Checkout this commit" (detached HEAD) in the commit context menu** — inspect/build an old commit's exact tree without manually creating a branch.
  - _Change:_ git.rs: add `checkout_commit(repo_path, sha)` running `git checkout <sha>`, surfacing dirty-tree failures as Err; register in main.rs. commands.ts: add `gitApi.checkoutCommit`. CommitList: `onCheckoutCommit` prop + item. MainLayout: handler → checkout then refreshStatus/Branches/Log. NOTE: leaves a detached HEAD — confirm the Header/branch label renders the detached state acceptably before shipping.  ·  _Files:_ `tauri-app/src-tauri/src/commands/git.rs`, `tauri-app/src-tauri/src/main.rs`, `tauri-app/src/lib/api/commands.ts`, `tauri-app/src/lib/components/CommitList.svelte`, `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium
- [ ] **+N/-M line summary in the commit detail header** — gauge a commit's size at a glance without opening the diff.
  - _Change:_ Extend `get_commit_files` (or add a sibling `get_commit_stat`) to run `git show --numstat --format= <sha>` returning `{added,deleted}` totals; thread into repoState (new `activeCommitStat` field) and pass to CommitDetail as props rendered beside the files-count. Avoid summing per-file by re-fetching each diff. Touches Rust + store + types + view — upper edge of small-medium; if numstat parse is non-trivial, fall back to just file count.  ·  _Files:_ `tauri-app/src-tauri/src/commands/git.rs`, `tauri-app/src/lib/api/commands.ts`, `tauri-app/src/lib/stores/repo.ts`, `tauri-app/src/lib/views/MainLayout.svelte`, `tauri-app/src/lib/views/CommitDetail.svelte`  ·  _Effort:_ small-medium
- [ ] **File-row context menu inside commit detail (copy path / view on GitHub)** — grab a changed file's path or open it on GitHub without leaving the commit view.
  - _Change:_ FileList.svelte: add optional `onContextMenu?(file, e)` prop + an `oncontextmenu` row handler. In MainLayout's commit-detail FileList instance, open the existing ContextMenu with Copy path (navigator.clipboard, `FileEntry.path`) and — once the opener plugin lands — "View file on GitHub" (`…/blob/{sha}/{path}`). Copy path works standalone; the GitHub action depends on the opener foundation (see App shell). Render the menu outside any transformed wrapper.  ·  _Files:_ `tauri-app/src/lib/components/FileList.svelte`, `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium

## Branches

- [ ] **Branch-picker filter/search input** — find a branch instantly in repos with many branches instead of scrolling a flat list.
  - _Change:_ Add `let filter = $state('')` and a search `<input use:autofocus>` at the top of the browse-mode .branch-list. Change derivations to filter by `b.name.toLowerCase().includes(filter.toLowerCase().trim())` for local + remote. Hide a section header when its filtered array is empty; show "No branches match" when both are empty. Reset `filter=''` in handleCancel/handleSwitch. Pure frontend.  ·  _Files:_ `tauri-app/src/lib/views/BranchDropdown.svelte`  ·  _Effort:_ small
- [ ] **Branch right-click context menu (Merge / Rename / Copy name / Delete)** — one gesture for every branch action instead of only a hover X; unlocks rename which has zero UI today.
  - _Change:_ Import ContextMenu (copy CommitList's pattern). Add `let ctxMenu = $state<{x,y,branch}|null>(null)` + an `oncontextmenu` handler per row. Build a `$derived` ContextMenuItem[]: "Merge into current" (non-current local → `onMerge`), "Rename…" (local → inline rename form → `onRename(old,new)`), "Copy branch name" (clipboard), separator, "Delete…" (destructive → existing handleDeleteClick). Render `<ContextMenu>` at the component root. MainLayout adds `handleRenameBranch` calling `gitApi.renameBranch` (already wrapped, no caller today) then refreshBranches/Status. Reuse create-form styling for the rename input.  ·  _Files:_ `tauri-app/src/lib/views/BranchDropdown.svelte`, `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium
- [ ] **Create-branch base picker + duplicate-name validation** — branch off any branch (not just HEAD) and avoid the raw git error when a name collides.
  - _Change:_ (a) Add a base-branch `<select>` to the create form populated from `localBranches`, default `currentBranch`; change `onCreate` to `(name, base)`. (b) Add `nameTaken = $derived(branches.some(b => !b.is_remote && b.name === newBranchName.trim()))`; disable Create + show inline message when taken or empty. In MainLayout `handleCreateBranch(name, base)` pass `base` as start_point (backend + commands.ts already support it). Keep auto-switch-after-create.  ·  _Files:_ `tauri-app/src/lib/views/BranchDropdown.svelte`, `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium
- [ ] **Delete-branch "also delete on remote" checkbox + irreversibility warning** — delete local + remote in one step with a clear warning, instead of a bare confirm and a leftover remote branch.
  - _Change:_ Delete mode: add "This action cannot be undone." and, when `branches.some(b => b.is_remote && b.name.endsWith('/' + deleteBranchName))`, a checkbox bound to `let alsoDeleteRemote = $state(false)`. Change `onDelete` to `(name, alsoRemote)`. MainLayout: keep `deleteBranch`, then `if (alsoRemote) { const remote = await gitApi.getRemote(repoPath); await gitApi.deleteRemoteBranch(repoPath, remote, name) }` (both already wrapped, no caller today), then refreshBranches. Reset the checkbox in handleCancel.  ·  _Files:_ `tauri-app/src/lib/views/BranchDropdown.svelte`, `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium

## Sync & force-push

- [ ] **Only offer Force push when the branch has actually diverged** — stops force-pushing a healthy ahead-only branch; force push appears only when it's the right tool.
  - _Change:_ Add `const diverged = $derived(hasUpstream && ahead > 0 && behind > 0)`. Change the force-push menu entry's `enabled` to `diverged && !isPushing`; when not diverged, omit it or grey it with a "Branch has not diverged — pull first" subtitle. Keep `--force-with-lease` semantics. Frontend-only derivation off existing ahead/behind.  ·  _Files:_ `tauri-app/src/lib/components/Header.svelte`  ·  _Effort:_ small
- [ ] **Friendly "newer commits on remote — fetch first" message on push rejection** — turns the cryptic "! [rejected] non-fast-forward" stderr into a plain-language next step.
  - _Change:_ In `handlePush`'s catch, match the error (`/non-fast-forward|fetch first|tip of your current branch is behind/i`) and replace the message with "The remote has commits you don't have yet. Pull (or Fetch) before pushing." before setting `repoState.error`. Keep raw stderr for other failures. Pairs with the retry/fetch wins. Pure frontend string check.  ·  _Files:_ `tauri-app/src/lib/components/Header.svelte`  ·  _Effort:_ small
- [ ] **Defensive status refresh after a FAILED sync op** — toolbar counts and the MERGING badge stay accurate after a failed pull/push instead of showing pre-op state until the next poll.
  - _Change:_ Call `await handleRefresh()` (or the silent `refreshStatus` via the `onSynced` prop) inside the catch blocks of `handlePull`/`handlePush`/`handleForcePush`. `handleRefresh` swallows its own errors, so it's safe. Trivial once those catch blocks are touched for the friendly-message + retry items — do all three together.  ·  _Files:_ `tauri-app/src/lib/components/Header.svelte`  ·  _Effort:_ small
- [ ] **Force-push confirm: inline preview of what gets overwritten** — before the irreversible action, show "origin/branch has N commit(s) you don't have — they will be discarded" instead of a generic warning.
  - _Change:_ Add `ahead`/`behind` props to ForcePushConfirm.svelte and pass them from Header's `<ForcePushConfirm>` (Header already derives both). When `behind>0`, render the discard/replace counts using the existing .muted paragraph styling. Pure prop threading + template text.  ·  _Files:_ `tauri-app/src/lib/components/ForcePushConfirm.svelte`, `tauri-app/src/lib/components/Header.svelte`  ·  _Effort:_ small
- [ ] **First-class manual Fetch button + "Last fetched N min ago" subtitle** — pull remote commits into ahead/behind on demand and always see how stale the view is; the refresh button only reads local status today.
  - _Change:_ Add `lastFetched: number|null` to RepoState (default null). In Header add `handleFetch()` = `getRemote` → `gitApi.fetch` → `handleRefresh`, setting `lastFetched: Date.now()` on success; also set it in MainLayout's `performAutoFetch`. Give the existing refresh icon-button `onclick={handleFetch}` and a `title` with a relative-time string ("Never fetched" when null). Optional tiny subtitle next to the up-to-date dot.  ·  _Files:_ `tauri-app/src/lib/components/Header.svelte`, `tauri-app/src/lib/stores/repo.ts`, `tauri-app/src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium

## Settings & prompts

- [ ] **Commit-length-warning toggle** — lets users who don't care about the 72-char convention hide the nag.
  - _Change:_ Add `#[serde(default = "default_true")] show_commit_length_warning: bool` to Config (config.rs) + commands.ts. Add a checkbox in SettingsOverlay (Git or Prompts section). In CommitMessage.svelte wrap the char-count span in `{#if $config?.show_commit_length_warning !== false}`.  ·  _Files:_ `tauri-app/src-tauri/src/commands/config.rs`, `tauri-app/src/lib/api/commands.ts`, `tauri-app/src/lib/views/SettingsOverlay.svelte`, `tauri-app/src/lib/components/CommitMessage.svelte`  ·  _Effort:_ small
- [ ] **Confirm-before-undo-commit dialog, gated by a pref** — stops an irreversible top-commit undo from firing on a single context-menu click.
  - _Change:_ Add `#[serde(default = "default_true")] confirm_undo_commit: bool` to Config + commands.ts. In MainLayout add `let undoTarget = $state<CommitInfo|null>(null)`; `handleUndoCommit` becomes: if `$config?.confirm_undo_commit !== false` set `undoTarget` (open dialog) else run the extracted `doUndo(commit)`. Render a tiny inline confirm (clone BranchDropdown's delete-confirm or ForcePushConfirm) with onConfirm=doUndo. Add the checkbox under a new Prompts section. (Composes with the dirty-tree warning under Changes.)  ·  _Files:_ `tauri-app/src-tauri/src/commands/config.rs`, `tauri-app/src/lib/api/commands.ts`, `tauri-app/src/lib/views/MainLayout.svelte`, `tauri-app/src/lib/views/SettingsOverlay.svelte`  ·  _Effort:_ small-medium
- [ ] **Confirm-before-AI-overwrite of an existing commit message, gated by a pref** — prevents one Generate click from wiping a message the user already typed.
  - _Change:_ Add `#[serde(default = "default_true")] confirm_commit_message_override: bool` to Config + commands.ts. In CommitMessage.svelte, before generating, if `(summary.trim() || description.trim())` and `$config?.confirm_commit_message_override !== false`, set `let pendingGenerate = $state(false)` and show a tiny inline confirm (reuse delete-confirm style); on confirm call the existing `handleGenerate`. Add the matching checkbox in the Prompts section. (Same mechanism as the AI-overwrite confirm under Changes — this is the pref-gated version.)  ·  _Files:_ `tauri-app/src-tauri/src/commands/config.rs`, `tauri-app/src/lib/api/commands.ts`, `tauri-app/src/lib/components/CommitMessage.svelte`, `tauri-app/src/lib/views/SettingsOverlay.svelte`  ·  _Effort:_ small-medium
- [ ] **Prompts settings section with per-action "don't ask again" toggles** — one place to silence the safety dialogs power users find repetitive.
  - _Change:_ Add `confirm_force_push` + `confirm_undo_commit` (`#[serde(default = "default_true")]`) to Config + commands.ts. Add a `<h3>Prompts</h3>` block in SettingsOverlay with checkboxes bound to each. Wire force-push: in Header's force-push menu action, if `confirm_force_push` is false call `handleForcePush()` directly, else open the dialog. Scope to force-push + undo only; do NOT add discard/stash toggles (no such actions exist yet). (Umbrella for the per-pref items above.)  ·  _Files:_ `tauri-app/src-tauri/src/commands/config.rs`, `tauri-app/src/lib/api/commands.ts`, `tauri-app/src/lib/views/SettingsOverlay.svelte`, `tauri-app/src/lib/components/Header.svelte`  ·  _Effort:_ small-medium
- [ ] **Theme "System (follow OS)" option with live OS-change tracking** — app matches the user's OS appearance automatically, day and night.
  - _Change:_ Add `<option value="system">System</option>` to the theme select. In config.ts `applyTheme`, when `theme==='system'` resolve via `window.matchMedia('(prefers-color-scheme: dark)').matches` and set `dataset.theme`. Add a module-level matchMedia listener (registered once, idempotent) that re-applies when it fires AND the stored `config.theme==='system'`. No Rust change beyond accepting the string.  ·  _Files:_ `tauri-app/src/lib/views/SettingsOverlay.svelte`, `tauri-app/src/lib/stores/config.ts`  ·  _Effort:_ small-medium

## App shell & refresh

- [ ] **Reflect "\<repo\> — \<branch\>" in the OS window title** — tells you which repo/branch you're on from the OS window switcher and taskbar.
  - _Change:_ `import { getCurrentWindow } from '@tauri-apps/api/window'`; add a `$effect` reacting to repoName + `$repoState.status.branch` calling `getCurrentWindow().setTitle(\`${name} — ${branch}\`)` (fallback "leogit"). Reuse the basename/repoIdentifier logic from Header. Fire-and-forget with `.catch(() => {})`. No new dependency or permission (`core:window:default` already granted).  ·  _Files:_ `src/lib/views/MainLayout.svelte`  ·  _Effort:_ small
- [ ] **Refresh on real OS window-focus (catch alt-tab), not just tab visibility** — status/branches/ahead-behind are fresh the instant you switch back from a terminal.
  - _Change:_ In MainLayout `onMount`, `const unlisten = await getCurrentWindow().onFocusChanged(({ payload: focused }) => { if (focused && $appState.phase === 'main') { refreshStatus({silent:true}); pollHeadSha(); refreshBranches() } })`; call `unlisten()` in cleanup. Keep `visibilitychange`, and add `refreshBranches()` to `handleVisibilityChange` too.  ·  _Files:_ `src/lib/views/MainLayout.svelte`  ·  _Effort:_ small
- [ ] **Platform-aware keyboard shortcut labels in HelpOverlay** — shortcuts read correctly per-OS (⌘ on macOS) instead of the clunky "Ctrl/Cmd + X" dual label.
  - _Change:_ Detect mac via `navigator.platform`/`userAgent` once, and render the modifier as '⌘' on mac vs 'Ctrl' elsewhere; replace the literal 'Ctrl/Cmd + ' prefixes with a small mod string. Bindings were verified accurate — purely presentational.  ·  _Files:_ `src/lib/views/HelpOverlay.svelte`  ·  _Effort:_ small
- [ ] **Fix README's false pull-request support claim** — stops the docs promising a feature the app can't do (no PR commands or UI exist).
  - _Change:_ Edit the README bullet that claims "Open pull requests through the GitHub CLI…" — delete it or replace with the truthful capability ("Detects `gh` auth for future GitHub features"). Doc-only edit.  ·  _Files:_ `README.md`  ·  _Effort:_ small
- [ ] **Add `tauri-plugin-opener` as the foundation for open-in-browser / reveal-in-Finder** — unlocks every external-open action (browser, Finder, editor) with one dependency; leogit currently has no way to open a URL or path.
  - _Change:_ Add `tauri-plugin-opener = "2"` to src-tauri/Cargo.toml, `.plugin(tauri_plugin_opener::init())` to the Builder in main.rs, `opener:default` (+ `opener:allow-open-url` / `opener:allow-reveal-item-in-dir`) to capabilities/default.json, and `@tauri-apps/plugin-opener` to package.json. Enabling step only — no UI yet. **The two consumer items below depend on this; the commit-detail "View file on GitHub" action also depends on it.**  ·  _Files:_ `src-tauri/Cargo.toml`, `src-tauri/src/main.rs`, `src-tauri/capabilities/default.json`, `package.json`  ·  _Effort:_ small-medium
- [ ] **"View commit on GitHub" in the commit context menu** _(depends on the opener foundation)_ — jump from a local commit straight to its GitHub page.
  - _Change:_ Pass the current repoIdentifier (owner/name from the `repoIdentifiers` store, keyed by repoPath, same as Header) into CommitList. Add a separator + `{ label: 'View on GitHub', enabled: identifier !== null, action: () => openUrl(\`https://github.com/${owner}/${name}/commit/${commit.sha}\`) }` using `import { openUrl } from '@tauri-apps/plugin-opener'`. (Host assumes github.com — gate on identifier presence.)  ·  _Files:_ `src/lib/components/CommitList.svelte`, `src/lib/views/MainLayout.svelte`  ·  _Effort:_ small
- [ ] **"Reveal in Finder" / "View on GitHub" on the repo chip menu** _(depends on the opener foundation)_ — one click to the repo folder or its GitHub homepage, no path copy-paste.
  - _Change:_ Add a context menu (reuse ContextMenu.svelte) on the repo chip's `oncontextmenu` with "Reveal in Finder" → `revealItemInDir($appState.repoPath)` and "View on GitHub" (enabled when repoIdentifier) → `openUrl(\`https://github.com/${owner}/${name}\`)`, both from `@tauri-apps/plugin-opener`. Header already imports ContextMenu and has the identifier.  ·  _Files:_ `src/lib/components/Header.svelte`  ·  _Effort:_ small
- [ ] **Transient success banner for merge / pull results** — positive confirmation that a merge/pull actually did something, with branch names, instead of silent success.
  - _Change:_ Build a minimal Toast.svelte (fixed bottom-center, auto-dismiss ~4-5s, single message, dismiss-on-click) + a tiny `toast` writable store (`writable<string|null>` + `show(msg)` that sets then clears via setTimeout). Render `<Toast/>` once in MainLayout. Call `toast.show(\`Merged ${mergeTarget} into ${branch}\`)` after a successful merge and `\`Pulled ${delta} commit(s)\`` after pull (delta from behind-before). Keep it to ONE primitive + store — stop if it grows per-type banner components.  ·  _Files:_ `src/lib/components/Toast.svelte`, `src/lib/stores/toast.ts`, `src/lib/views/MainLayout.svelte`  ·  _Effort:_ small-medium

## Suggested first sprint

Most daily-use improvement per effort — lead with the already-built ones (just wiring), then the cheapest high-traffic polish.

1. **Open the already-wired Merge overlay from the branch picker** `[already built]` — biggest single win; resurrects an entire dead feature for ~10 lines.
2. **Refresh history + branches inline after push/pull** `[already built]` + **after merge** `[already built]` — kills the visible 2s sync lag everywhere (do both, they share the pattern).
3. **Wire ErrorModal's Retry button** `[already built]` — one-click recovery from flaky push/pull/fetch.
4. **Commit button label "Commit to \<branch\>" + disabled-reason tooltip + spinner** — every commit touches this; clarity + in-flight feedback.
5. **Single-file "Create/Delete/Update \<file\>" placeholder + commit-when-blank** — makes the most common commit (one file) zero-typing.
6. **Per-file +N/-M counters in the diff header** + **"N changed files" cap at 300+** — instant change-size sense in the two most-viewed surfaces, both tiny CSS/derive edits.
